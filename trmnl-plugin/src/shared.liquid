{%- comment -%}
  Each layout is self-contained (it repeats the small date→index selection
  block), so no shared logic is required here. Kept for completeness of the
  expected plugin file structure.
{%- endcomment -%}
