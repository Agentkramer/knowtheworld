{%- comment -%}
  No shared markup or styles needed: each layout is self-contained and the map
  is a single inline SVG that stacks the basemap + overlay via its viewBox, so
  there is no custom CSS. Kept for the expected plugin file structure.
{%- endcomment -%}
